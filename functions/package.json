{
  "name": "f1x-functions",
  "description": "Firebase Cloud Functions backend for F1X Chrome Extension",
  "scripts": {
    "lint": "eslint src/",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "private": true
}
